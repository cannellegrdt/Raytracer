# Main scene without camera, imports sub

imports = (
    { path = "scenes/tests/sub.cfg"; }
);

primitives = {
    spheres = (
        {
            x = 2.0; y = 0.0; z = 0.0;
            r = 0.5;
            material = { type = "flat"; color = { r = 0.2; g = 0.8; b = 0.2; }; };
        }
    );
};

lights = {
    directional = (
        { direction = { x = 0.0; y = -1.0; z = 1.0; }; color = { r = 1.0; g = 1.0; b = 1.0; }; intensity = 0.7; }
    );
};